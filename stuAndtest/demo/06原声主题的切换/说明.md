# 原生的主题切换
- css主题，如下，这个写法挺好的
  ```js
    /* 默认使用白天主题变量 */
    :root {
      /* 主色调：宝石紫（呼应奖励场景，活力且高级） */
      --primary-color: #6831ff;
      --primary-light: #8955fd;
      --primary-dark: #4c3195;
      // ....
    }

    /* 暗色主题变量 */
    .dark-theme {
      /* 主色调：深宝石紫（呼应暗色主题，高级不刺眼） */
      --primary-color: #8955fd;
      --primary-light: #b794f4;
      --primary-dark: #4c3195;
      // ...
    }
  ```
- js的切换（有浏览器直接读取或用户按钮点击切换）
  ```js
    // 主题切换功能
    const themeToggle = document.getElementById("themeToggle");
    const html = document.documentElement;

    // 检查本地存储中的主题偏好
    // 浏览器本身可以设置主题，和这个页面无关，如果没有设置暗色，window.matchMedia("").matches即返回false，执行白色主题
    if (
      localStorage.getItem("theme") === "dark" ||
      (!localStorage.getItem("theme") &&
        window.matchMedia("(prefers-color-scheme: dark)").matches)
    ) {
      html.classList.add("dark-theme");
      themeToggle.textContent = "☀️";
    } else {
      html.classList.remove("dark-theme");
      themeToggle.textContent = "🌙";
    }

    // 主题切换事件 （按钮直接切换，暗色主题css在白色主题下面，达到了覆盖作用)
    themeToggle.addEventListener("click", () => {
      if (html.classList.contains("dark-theme")) {
        html.classList.remove("dark-theme");
        themeToggle.textContent = "🌙";
        localStorage.setItem("theme", "light");
      } else {
        html.classList.add("dark-theme");
        themeToggle.textContent = "☀️";
        localStorage.setItem("theme", "dark");
      }
    });
  ```